// this is probably a bad way of doing this, but its easy. i think?
// these are values that i need to use globaly but dont require a rerender if they are changed
const Globals = { isSolo: false };
export default Globals;
